export interface PathColors {
  folderPath: string;
  color?: string;
  badge?: string;
  nodeOnly?: boolean;
}

export interface PathsColors {
  folderPath: string[];
  color?: string;
  badge?: string;
  nodeOnly?: boolean;
}
